# 0.2.3 更新改动扫描（按文件清单）

> 依据 `0.2.3` 改动说明进行的本地影响排查。仅保留需要落地或复核的文件，未改动项与重复项已清理。

## 说明
- 仅列出当前已确认涉及的文件。
- 所有条目统一加上 `[已分析]` 前缀。
- 每个文件仅保留一条记录，改动点在下方按要点列出。

### [已分析] `src/features/plugins/PluginCard.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`：涉及 `卸载`、`更新`、`安装`。

### [已分析] `src/features/plugins/UploadPlugin.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。

### [已分析] `src/features/project-editor/components/CategoryView.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。

### [已分析] `src/features/project-editor/components/ProjectOverview.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。
- `RollingBox` 显式处理滚动方向（避免隐式默认导致交互不一致）。

### [已分析] `src/features/project-editor/components/ProjectCoverPickerModal.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。
- `Select` 回调当前为 `onChange`，建议核对并兼容 `onValueChange`。

### [已分析] `src/app/mobile/pages/MobileProjectHome.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`（`新建词条`、`AI 讨论`）。

### [已分析] `src/features/project-editor/components/EntryRelationCreator.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。
- `Select` 回调当前为 `onChange`，建议核对并兼容 `onValueChange`。

### [已分析] `src/features/project-editor/components/ProjectContradictionPanel.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。
- `Select` 回调形态需核对兼容边界。
- `RollingBox` 侧建议补充 `axis="y"`。

### [已分析] `src/features/project-editor/components/ProjectTimeline.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。
- `RollingBox` 已为 `axis="x"`，请复核方向与交互最终一致性。

### [已分析] `src/features/projects/components/ProjectCreator.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。

### [已分析] `src/features/relation-graph/components/ProjectRelationGraph.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。

### [已分析] `src/pages/Idea.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。
- `Select` 回调当前多数为 `onChange`，建议核对并统一为 `onValueChange`。

### [已分析] `src/pages/ProjectList.tsx`
- `flowcloudai-ui` 的 `Button` 组件补充 `type="button"`。
- `RollingBox` 未显式滚动方向，建议补充。

### [已分析] `src/pages/Settings.tsx`
- `flowcloudai-ui` 的 `Button` 组件批量补充 `type="button"`。
- `Select` 回调当前多数为 `onChange`，建议统一为 `onValueChange`。

# app_main 前端采用功能模块化单体

`app_main` 前端采用功能模块化单体：`app` 只负责组装桌面端、移动端应用壳与顶层导航，`features` 拥有业务用例和界面，`api` 只承载 IPC 与平台能力适配，`shared` 只存放无业务归属的通用能力。功能模块不得直接复用另一个功能模块的界面内部实现；跨功能的稳定业务能力应归属到明确的公共边界。该方向保留现有 React + Tauri 单体部署，避免多包或微前端拆分成本，同时用单向依赖控制边界漂移。
